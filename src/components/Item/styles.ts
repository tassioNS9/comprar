import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
    gap: 7,
  },
  description: {
    flex: 1,
    fontSize: 14,
    fontWeight: 600,
    color: "#4F4F4F",
  },
});

export default styles;
